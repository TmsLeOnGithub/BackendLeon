
export default class ChatFactory {
    constructor({ timestamp,autor,text}) {
        this.timestamp = timestamp
        this.autor = autor
        this.texto = this.texto
    }
}

export function asDto(chat) {
    if (Array.isArray(producto))
        return pers.map(p => new chatDto(p))
    else
        return new chatDto(producto)
}